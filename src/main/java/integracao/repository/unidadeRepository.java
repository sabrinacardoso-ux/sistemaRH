package integracao.repository;

import integracao.model.unidade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface unidadeRepository extends JpaRepository<unidade, Long> {
}
